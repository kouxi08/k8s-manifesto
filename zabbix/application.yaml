apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zabbix-application
  namespace: argocd

spec:
  destination:
    namespace: zabbix
    server: 'https://kubernetes.default.svc'
  source:
    path: zabbix
    repoURL: https://github.com/kouxi08/k8s-manifesto.git
    targetRevision: main
  project: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
